# ClovaLink Production Environment Configuration
# Copy this file to .env for production deployment
# WARNING: Never commit the actual .env file to version control!

# Application
NODE_ENV=production

# Domain Configuration
DOMAIN=your-domain.com

# Database Configuration
# Update with your production PostgreSQL credentials
DATABASE_URL=postgresql://postgres:your-strong-password-here@db:5432/clovalink?schema=public

# NextAuth Configuration
# Generate a strong secret: openssl rand -base64 32
NEXTAUTH_SECRET=generate-a-strong-secret-here
NEXTAUTH_URL=https://your-domain.com

# Document Encryption Key (CRITICAL)
# Generate a key: node generate-encryption-key.js
# This key is used to encrypt all documents at rest
# NEVER SHARE THIS KEY - Back it up securely offline!
# If you lose this key, you CANNOT decrypt existing files!
ENCRYPTION_KEY=generate-and-put-your-production-key-here

# Rust Crypto Acceleration (RECOMMENDED for production)
# Enable high-performance Rust crypto core for 5-10x faster encryption
USE_RUST_CRYPTO=true

# Redis Configuration (recommended for production)
# Used for rate limiting and session management
REDIS_URL=redis://redis:6379

# File Upload Configuration
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=104857600  # 100MB in bytes
ALLOWED_MIME_TYPES=application/pdf,image/*,text/*,application/vnd.*

# SMTP Configuration (required for email notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@your-domain.com

# Server Configuration
PORT=3000
HOSTNAME=0.0.0.0

# Logging Configuration
LOG_LEVEL=info  # Options: trace, debug, info, warn, error, fatal

# Cron Jobs (optional)
# Secret key for authenticating cron job requests
CRON_SECRET=generate-another-random-secret-here 