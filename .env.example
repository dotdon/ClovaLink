# ClovaLink Environment Variables Example
# Copy this file to .env and fill in your actual values
# WARNING: Never commit the actual .env file to version control!

# Database Configuration
# For local development with Podman, use the default below
# For production, use your PostgreSQL connection string
DATABASE_URL=postgresql://postgres:postgres@db:5432/clovalink?schema=public

# NextAuth Configuration
# Application URL - change to your domain in production
NEXTAUTH_URL=http://localhost:3000

# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here

# SMTP Configuration (optional - for email features)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@example.com

# Document Encryption Key (CRITICAL)
# Generate a key: node generate-encryption-key.js
# This key is used to encrypt all documents at rest
# NEVER SHARE THIS KEY - Back it up securely!
# If you lose this key, you CANNOT decrypt existing files!
ENCRYPTION_KEY=generate-and-put-your-key-here

# Rust Crypto Acceleration (optional - production recommended)
# Set to true to use high-performance Rust crypto core
# Requires rust-core to be compiled (automatic in Containerfile)
# Provides 5-10x faster encryption and 10-20x faster hashing
USE_RUST_CRYPTO=false

# Redis Configuration (optional - for rate limiting)
# If not set, will use in-memory storage for rate limiting
# REDIS_URL=redis://localhost:6379

# Server Configuration (optional)
# PORT=3000
# HOSTNAME=localhost

# Upload Configuration (optional)
# UPLOAD_DIR=./uploads
# MAX_FILE_SIZE=104857600  # 100MB in bytes
# ALLOWED_MIME_TYPES=application/pdf,image/*,text/*

# Logging Configuration (optional)
# LOG_LEVEL=info  # Options: trace, debug, info, warn, error, fatal

# Cron Jobs (optional - for scheduled tasks)
# Secret key for authenticating cron job requests
# CRON_SECRET=your-cron-secret-here
